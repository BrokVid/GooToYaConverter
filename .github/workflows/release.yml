name: Build and Release Windows exe

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt

      - name: Build exe (PyInstaller, OneFile)
        run: |
          .\build_exe.bat

      - name: Create ZIP archive
        run: |
          Compress-Archive -Path "dist\GoogleToYandexWeb.exe" -DestinationPath "GooToYaConverter-${{ github.ref_name }}.zip"

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            GooToYaConverter-${{ github.ref_name }}.zip
          body: |
            ## 🗺️ GooToYaConverter ${{ github.ref_name }}
            
            Конвертер координат Google Maps → Яндекс.Карты (Единая сборка)
            
            ### 📥 Установка
            1. Скачайте архив `GooToYaConverter-${{ github.ref_name }}.zip`
            2. Распакуйте `GoogleToYandexWeb.exe`
            3. Запустите файл
            
            > ⚙️ Настройки и калибровка хранятся в папке `%APPDATA%\GooToYaConverter`
            
            ### ✨ Возможности
            - Автоматическая конвертация координат из буфера обмена
            - Калибровка для вашего города
            - Интерактивная карта всех откалиброванных точек
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
